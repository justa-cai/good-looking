# good-looking

浏览器端人脸颜值分析。上传照片 → 本地推理 → 出分，**照片永不上传服务器**。

## 硬约束

1. **纯前端推理**：所有推理在浏览器内完成，没有后端服务，没有任何上传接口。
2. **静态托管**：产物部署到 GitHub Pages，只能有静态文件。因此——
   - 不能用需要服务端的路由（用 hash 路由或单页）；
   - Vite `base` 必须是 `/<repo-name>/`，否则 GitHub Pages 子路径下资源全 404。
3. **可公开再分发**：仓库是公开的，任何入库的代码/权重/资产都必须许可证干净。

## 两条推理路线

### 路线 A：几何打分（MediaPipe FaceMesh）— 主力，零权重

`图片来源 → MediaPipe Face Landmarker → 478 个关键点 → 纯 TS 几何引擎 → 分项分 + 总分 → 可视化叠加`

- 模型与 WASM 全部来自 CDN，**仓库不含任何权重**。
- 输出是对**面部比例**的度量（三庭五眼、对称性、canthal tilt、midface ratio 等），
  不是「学出来的审美」。确定性：同图同分，可比对、可复现。
- 参考实现：`Blueturboguy07/freeharmony`（AGPL-3.0 —— **只读参考，不要拷代码进本仓库**，
  本仓库要保持许可证自由）。

### 路线 B：学习模型（ViT 二分类，Apache-2.0）

`图片来源 → 人脸检测 + 仿射对齐 → 224×224 → ViT ONNX (WebGPU/WASM) → 好看概率`

- 模型：`dima806/attractive_faces_celebs_detection`，**Apache-2.0**，base 是
  `google/vit-base-patch16-224-in21k`（同样 Apache-2.0）。
- 输出：`attractive` / `not attractive` 二分类 softmax，不是 1-5 连续分。
- **不对齐会显著影响结果**，必须走对齐。

### 两个**输入路径**，不是第三条路线

上面两条路线各有两条入口：**上传照片**，和**摄像头实时取景**。

摄像头那条是**取景器**，不是第三种测量：实时循环里只跑精简版（逐帧检测 + 质量校验 +
打分 + 平滑），跳过模糊拒判、不跑路线 B；用户点「冻结这一帧」之后，那一帧走的是和上传
**完全相同**的完整流程，**以那一次的结果为准**。实时与冻结的分差是必然的（同帧实测
中位 1.87 分），原因和全部实测数据见「摄像头实时」一节。

## 已核实的事实（2026-09-12）

不要凭记忆改动这些，改之前先重新核实。

| 项 | 值 | 核实方式 |
|---|---|---|
| `@mediapipe/tasks-vision` 最新版 | `1.0.1` | `npm view @mediapipe/tasks-vision version` |
| `onnxruntime-web` 最新版 | `1.29.0` | `npm view onnxruntime-web version` |
| `vite` 最新版 | `8.3.0` | `npm view vite version` |
| `typescript` 最新版 | `7.0.2`（原生编译器） | `npm view typescript dist-tags` |
| 包管理器 | `pnpm` 10.12.1 | 本地 `pnpm -v`，CI 里同版本 |
| ViT 模型许可证 | `apache-2.0` | HF API `cardData.license` |
| ViT 输入 | 224×224，mean/std 均 0.5，rescale 1/255，bicubic | `preprocessor_config.json` |
| ViT 标签 | 2 类（attractive / not attractive），报告准确率 83.8% | 模型卡 classification report |
| ViT 权重文件 | 根目录 `model.safetensors`（另有 checkpoint-149/3083 等中间检查点） | HF API `siblings` |

### MediaPipe 资源 CDN（已核实可访问，返回 206）

- wasm 目录：`https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@1.0.1/wasm/`
- 模型：`https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task`（3.6 MB）

版本号写在 URL 里，跟着 npm 包的版本一起升，不要漏改。

### ⚠️ 本机浏览器的 GPU 性能数据不可信

开发机上的 Chrome（playwright / CDP 常驻）跑在 `--headless --disable-gpu --use-gl=disabled` 下，
WebGL renderer 实测是 **SwiftShader（纯软件光栅化）**，宿主那块 AMD Radeon HD 7850 没被用上。

所以：**在本机测出的任何「GPU 比 CPU 快/慢」的结论都不成立**，不要据此改代码。
需要 GPGPU 性能数据时必须换到有真实硬件加速的设备上测。

### 后端选择：运行时实测，不写死

`face/landmarker.ts` 默认 `delegate: 'auto'` —— 两个后端各建一次、各跑 3 帧计时，留下快的那个。

原因是没法预判：MediaPipe 的 GPU delegate 需要把结果从显存读回 CPU（`ReadPixels`），
在软件渲染下实测比 CPU **慢 4.7 倍**（217ms vs 46ms），但在真实硬件上又会反过来。
既然开发机测不出有效结论，就让它到用户的设备上自己量。代价是首次加载多约 1s。

实测（本机，SwiftShader）：
| 输入最长边 | 单帧耗时（CPU） |
|---|---|
| 1025px | 42 ms |
| 512px | 34 ms |
| 256px | 32 ms |

缩放对耗时影响很小（模型内部有自己的输入尺寸），所以 `MAX_DIM` 保持 1280，
换取更稳的关键点定位。

**给关键点做改动后务必验证两件事**：478 个点齐全；索引语义没串
（用 `tmp/` 里的脚本在真人照片上画出标注再肉眼核对，别只看数字）。

## 几何指标的校准（改测量式前必读）

`src/engine/calibration.ts` 里的均值/标准差/理想值**不是手填的**，是用
`src/engine/metrics.ts` 的测量式在一批真人照片上跑出来统计得到的。
流程见 `scripts/calibration/README.md`。

**改动任何测量式（换关键点、改投影方式、改归一化基准）都必须重跑校准。**
这类错误不会报错，只会让所有人看起来都偏丑或偏好看 —— 极难察觉。

当前校准样本：37 张美国国会官方肖像（公有领域），25-30 人，9 人有 2-3 张。
样本图片不入库（`tmp/faces/`，已 gitignore），用 `fetch_sample.py` 重新抓。

### 评分模型

```
分项分 = 100 · exp(−ln2 · (Δ/容差)²)      容差 = 人群标准差 × 1.5
综合分 = 各分组分的平均（分组等权，不是各指标等权）
```
偏离 0 得 100 分，偏离 = 容差 得 50 分。容差取 1.5σ 而不是 1σ，
否则一半的人每项都会低于 50 分，看起来像在惩罚普通人。

### 理想值有三种来源，界面上必须区分

| 来源 | 含义 | 判定 |
|---|---|---|
| `canon` | 符合公认面部比例 | 实测均值与经典约定差 ≤ 0.75σ |
| `empirical` | 接近人群平均（经典约定存在但实测系统性偏离） | 差 > 0.75σ |
| `typical` | 接近人群平均（本就没有公认标准） | — |

当前 7 个指标里，`upperOverMiddle`（三等分）与 `mouthOverNose`（嘴宽=1.5鼻宽）
的经典约定成立；其余 5 个是 `empirical`/`typical`。

`empirical` 的含义要说清楚：**整批人同方向偏离经典约定 2σ 以上，不可能来自人本身，
只能是测量系统的问题**（MediaPipe 的眼角点比解剖眼角靠内、鼻翼点比鼻翼外缘靠外）。
所以那些指标的分数是「像不像普通人」，不是「符不符合公认比例」。

### ⚠️ 单张照片的分数有 ±5~8 分的噪声

实测：同一个人不同照片的综合分极差在 **0.7 ~ 15.4 分**之间（9 人样本）。
差得多的那些（Wexton 49.1 vs 64.5）主要是姿态差异造成的。

所以：
- 不要把个位数的分差当成有意义的差异来解读或展示；
- 界面要明说这一点，别让用户拿两次分数对比；
- 这正是质量校验（拒判姿态过大的照片）必须做的原因。

### 已核实的引擎行为（2026-09-12）

- **确定性**：同一组关键点连算 5 次，综合分完全一致（浮点级）。
- **分数分布**（37 张样本）：均值 77.6，标准差 8.9，范围 49.1–93.5。
- 分项噪声占比（组内方差/总方差）：`faceWidthOverHeight` 12.6% 最可信，
  `lowerOverMiddle` 40.1% 最不可信 —— 后者在界面上要标出来。

## 头部姿态与输入质量

### 姿态只从变换矩阵取，不要自己拿关键点推

`src/face/pose.ts` 把 MediaPipe 的 4×4 变换矩阵（**行主序、行向量约定**，
平移在最后一行）分解成 yaw/pitch/roll。

试过用左右关键点的对称性当姿态判据，**噪声太大**：同一人不同照片的对称性差异
比不同人之间还大（关键点会随表情和光照漂移）。模型直接给的 3D 配准结果稳定得多。

roll 的符号用受控实验验证过：把图按 CSS `rotate(+θ)` 转，读数 ≈ +θ
（θ=10° 读数 8.6、θ=20° 读数 18.6、θ=-10° 读数 -11.5）。yaw/pitch 无法这样合成，
只验了量级合理（专业证件照都在 13° 以内）。

### ⚠️ 引擎不是「旋转不变」的 —— 转图会改变分数

引擎的测量式本身是旋转不变的（距离 + 沿中轴投影），但**实测转图会改变分数**：
同一张图转 0°/10°/20°/-10°，综合分是 80.1 / 81.2 / 76.7 / 70.0。

原因不在引擎，而在 MediaPipe：图一转，它给出的关键点位置就变了（模型不是旋转等变的，
加上重采样）。所以**姿态必须进质量校验**，不能因为「引擎设计上不受影响」就跳过。

依据：同一人不同照片的分数极差与姿态极差的相关系数 **0.91** —— 姿态是分数不稳的主因。

### 阈值及其依据（`src/face/quality.ts`）

| 判据 | 阈值 | 依据 |
|---|---|---|
| 姿态 | \|yaw/pitch/roll\| ≤ 20° | **安全网，不是精确刻度**。30 张专业证件照实测最大 9.8°/13.0°/8.8°，所以 20° 一定放过正常照；但样本里没有真正拍坏的照片，**找不到该拒判的临界点** |
| 瞳距下限 | ≥ 50 px | 逐步缩小照片重测：ipd ≥ 100px 时 \|Δ\| ≲ 2 分；50–70px 时 ≈ 4-5 分；≤ 35px 时 > 6 分，27px 到 13 分 |
| 瞳距警告 | < 100 px | 同上，偏差开始可见的位置 |
| 人脸出画 / 贴边 | 出画拒判，贴边（2%）警告 | 出画时边缘关键点是推算的，下巴一出画整个下庭就错了 |
| 清晰度下限 | ≥ 4.0（归一化拉普拉斯方差） | 见下方「模糊检测」 |
| 清晰度警告 | < 6.0 | 同上；约 8% 的清晰样本会落进这一档 |

**没有做闭眼检测**：实测眼睑开合度的个体差异极大（同一批人 0.17–0.46，差近 3 倍），
闭眼的和天生眼裂小的分不开，拿它拒判会大量误伤；而闭眼对几何比例几乎没影响。
不检查反而更正确。

### 模糊检测（`src/face/sharpness.ts`）

模糊是本项目里**唯一不会自然暴露自己的坏输入**：关键点之间的相对位置不变，
所以分数照样给得出来，只是每个点的定位抖动变大、分数不可信。
其它坏输入（无脸、侧脸、太小、被裁）都会在关键点上直接暴露，模糊不会。

判据是**拉普拉斯方差**（自动对焦领域的经典对焦度量），但做了两层归一化，
否则不同照片不可比：

- **分辨率归一**：把人脸包围盒（外扩 12%）重采样到固定 160×160 再算；
- **对比度归一**：除以同区域的亮度标准差。不然一张高对比度的糊照片方差可能更高。

归一之后剩下的量就是「细节密度」。实测分布（37 张官方肖像作清晰样本，
再对同一批图施加浏览器 CSS `blur(Npx)` 合成模糊样本）：

| 模糊半径 | 最小 | 中位 | 最大 |
|---|---|---|---|
| 0 px | 4.5 | 10.4 | 42.3 |
| 1 px | 0.6 | 5.5 | 13.3 |
| 2 px | 0.1 | 2.2 | 4.8 |
| 3 px | 0.1 | 1.1 | 2.5 |
| 4 px | 0.0 | 0.6 | 1.4 |

阈值取 **4.0 而不是分布交界处的 4.5**，是刻意留的余量：4.5 正好是这批
**专业肖像照**里最软的一张，普通用户随手拍的照片整体会更软，卡在 4.5 会误伤。
代价：`blur(2px)` 那档还剩约 11% 能过，`blur(3px)` 及以上 100% 拦下。

⚠️ **能力边界**：`blur(1px)` 这种轻微发虚和原本就清晰的照片**分不开**
（中位 5.5 vs 10.4，分布重叠严重）。这一关拦的是「明显糊」，不是「不够锐」。
不要试图调阈值去分这一档，分布本身不支撑。

⚠️ 测模糊**必须传位图**（`checkQuality(faces, bitmap)`）——要读原始像素。
省略位图时该检查被跳过，`report.sharpness === null`，表示「没测」，**不等于「清晰」**。
读不到像素（取不到 2D 上下文、包围盒落在图外）时同样返回 null 并跳过，不误判为清晰。

⚠️ 合成测试图必须用**浏览器自己的 CSS `blur(Npx)`**，不要用 PIL。
PIL 的 `GaussianBlur(radius)` 里 radius 就是 σ，和 CSS 的标定完全对不上：
PIL σ=1.0 实测 10.05，而 CSS `blur(2px)` 实测约 2–4 —— 差了 3 倍以上。
用错工具合成出来的样本会把阈值调到错的地方。

### 已核实：37 张正常证件照全部通过，异常输入全部正确拒判

异常用例在 `tmp/faces/quality_cases/`（临时，不入库）：无人脸、双人脸、
歪 30°、瞳距 36px、下巴被裁、`blur(2px)`、`blur(3px)`。

模糊用例由浏览器 CSS 滤镜在**缩放后的位图**上生成（`blur_css2.jpg` / `blur_css3.jpg`），
实测 3.93 / 1.94，都低于阈值 4.0 被拦下；对照组 `blur_css0.jpg` 实测 13.54 正常出分。

### 排查提示：图片取不到时的报错具有误导性

`createImageBitmap` 拿到 404 返回的 HTML 页面时报的是
`InvalidStateError: The source image could not be decoded`，
看起来像图片格式问题，其实是路径错了。**看到这个报错先查 HTTP 状态码**，
不要去找图片本身的毛病。这个坑踩过两次。

### ⚠️ GitHub Pages 没有 COOP/COEP，SharedArrayBuffer 不可用

这是本项目最重要的部署约束：**多线程 WASM 用不了**。

- GitHub Pages 不支持自定义响应头，所以拿不到 cross-origin isolation；
- 因此 `SharedArrayBuffer` 不存在，MediaPipe 和 ONNX Runtime 的**多线程后端都会退化为单线程**；
- 推论：dev server 也**故意不开** COOP/COEP（见 `vite.config.ts`）——保证本地和线上行为一致。
  否则会出现「本地飞快、线上慢十倍」这种只在部署后才暴露的问题；
- 后果：不要指望用 `numThreads` 调优；提速要靠 WebGPU（不依赖 SAB）和减小模型。


### ⚠️ 已排除的方案：SCUT-FBP5500

**不要再考虑这条路。** 原因（已核实）：

- `HCIILAB/SCUT-FBP5500-Database-Release` 仓库**没有任何 LICENSE 文件**
  （`LICENSE` / `LICENSE.md` / `LICENSE.txt` / `COPYING` 全部 404）→ 默认保留所有权利。
- 权重**不在仓库里**，托管在百度网盘（`pan.baidu.com/s/1OhyJsCMfAdeo8kIZd29yAw`，密码 `ateu`）
  和 Google Drive 上，无法在 CI 里自动获取。
- HF 上的镜像同样受限：`MnLgt/scut-fbp5500` 是 `license: other` 且**不含数据**（`usedStorage: 0`）。
- 仅限非商业研究用途，公开再分发权重有法律风险。

如果将来确实需要 1-5 连续分，正确做法是**开源训练代码、不发布权重**，让使用者自行获取数据集复现。

## 路线 B 的模型：已导出，但 ⚠️ 绝对概率不能当分数用

导出脚本 `scripts/model/export_onnx.py`，跑法与验证方式见文件头注释。

**导出结果（2026-09-12 核实）**：ONNX opset 17，327.6 MB，输入 `pixel_values`
`[batch,3,224,224]`（H/W 固定，batch 动态），输出 `logits` `[batch,2]`，**不含 softmax**
（softmax 放前端做，便于融合与量化）。PyTorch ↔ ONNX Runtime 的
`max|Δlogits|` 随机输入 ~5e-7、真实人脸照 2.1e-7 —— 数值上是同一个模型。

### ⚠️ 这个模型的输出有严重的人群偏移，不要直接线性映射到 0-100

用 `scripts/model/score_corpus.py` 在 37 张国会议员官方肖像上跑了一遍：

| 统计量 | 值 |
|---|---|
| 中位数 | **0.123** |
| 均值 / 标准差 | 0.231 / 0.232 |
| p05 / p95 | 0.017 / 0.649 |
| 判为 attractive（>0.5） | **7 / 37** |

这批全是正常、清晰、正脸的专业证件照，模型却给出「默认不好看」的判断。
原因不是 bug，是训练分布不匹配：模型在「名人颜值」图上训练，
和「中老年政治人物证件照」差得很远。

**但模型本身是稳定且有区分度的**，同一人多张照片的组内极差中位数只有 **0.026**，
而组间标准差是 **0.213**（约 8 倍）。也就是说：

- ✅ 可靠：同一批人里的**相对次序**；
- ❌ 不可靠：**绝对分数**。直接映射成 0-100 相当于给所有人一个稳定的低分，
  而且低的原因和脸无关。

所以界面（任务 #13）必须按「相对比较 / 排名」来呈现，不能和路线 A 的分数
并排成两个「/100 分」。具体形式待定，但任何形式的「模型颜值分」都要先解决这个偏移。

⚠️ 上面这些数只在这批样本（美国国会议员、以中老年、以欧美族裔为主）上成立，
换人群要重测。样本本身不入库，重测要先跑 `scripts/calibration/fetch_sample.py`。

## 量化结果（`scripts/model/quantize_onnx.py`，默认 int4）

| 模式 | 体积 | 排序一致性 ρ | argmax 一致 | ORT-web 加载 | ORT-web 推理 |
|---|---|---|---|---|---|
| fp32 | 327.6 MB | — | — | — | — |
| int8（动态，逐通道） | 83.6 MB | 0.9983 | 37/37 | 0.43 s | **0.87 s** |
| **int4（MatMulNBits，block=32）** | **54.2 MB** | 0.9967 | 37/37 | 0.34 s | 1.90 s |

**选 int4**：小 29 MB，而且精度反而更好（|ΔP(attr)| 中位 0.0039 vs int8 的 0.0128）——
block-wise 每 32 个权重一个 scale，比 int8 逐通道一整个 scale 更贴合权重的局部结构。

代价是单线程 WASM 上推理慢一倍（4-bit 读出时要反量化）。但这个数字来自
**SwiftShader 机器**（见上文「本机 GPU 性能数据不可信」），真实硬件上 4-bit 受
显存带宽约束，通常会反过来。**这个取舍需要在真实硬件上复测再定稿。**

⚠️ **`MatMulNBits` 在 ORT-web 1.29 的 WASM 后端确认可跑**（已实测，不是推测）。

⚠️ 第一个 session 创建会额外花一次 WASM 模块编译（实测 int8 那版报了 175 s），
第二次起才是真实加载耗时（0.3-0.4 s）。**比较加载时间时别被第一次的编译成本骗了** ——
把模型顺序换一下重测，数字就变了。

⚠️ **20-25 MB 的体积目标作废**：ViT-base 有 86M 参数，INT8 的下限就是 ~86 MB。
那个目标是把参数量估小了一个数量级得来的。真要更小只能换更小的骨干
（如 ViT-tiny 5.7M），那是换模型，不是压模型。

⚠️ 这一步**不要看 acc**：模型绝对概率有人群偏移，acc 本身没有意义。
要看的是量化前后的排序一致性（Spearman ρ）—— 界面只用它做相对比较。

## 人脸对齐（`src/face/align.ts`）—— 只为路线 B 服务

路线 B 的输入必须是对齐过的人脸裁剪，不能直接喂整张照片。理由是实测的。

做法是**相似变换**（旋转 + 等比缩放 + 平移），不做透视校正：
用两个虹膜中心把眼睛连线转成水平（只校正 roll），用瞳距当尺度基准，
把人脸按固定比例摆进裁剪框。裁剪框边长 = 瞳距 × 2.8，眼睛线放在 42% 高度处。

⚠️ **只校正 roll，不校正 yaw/pitch**。扳正侧脸需要 3D 模型，会引入插值失真，
还会凭空捏造侧脸看不到的那半边。侧脸本就不该出分 —— 那是 quality.ts 的 pose 关的事。

### 实测收益

**1. 对齐让模型对旋转几乎不敏感。** 同一张图转 -20°/-10°/0°/+10°/+20°
（每次重新检测关键点、重新对齐、再推理），对齐后的 P(attractive)：

| 样本 | -20° | -10° | 0° | +10° | +20° | 极差 |
|---|---|---|---|---|---|---|
| Pete Aguilar | 拒判 | 0.0554 | 0.0450 | 0.0470 | 0.0495 | 0.010 |
| Colin Allred | 0.1061 | 0.1403 | 0.1185 | 0.1308 | 拒判 | 0.034 |
| Jake Auchincloss | 拒判 | 0.7760 | 0.7458 | 0.7815 | 0.7719 | 0.036 |

对比几何路线：同一张图转 0°/20°/-10° 综合分是 80.1 / 76.7 / 70.0（掉了 10 分）。
**对齐确实解决了路线 B 的旋转敏感问题**，而且是可复现的。

**2. 对齐大幅降低「同一人不同照片」的离散度。** Auchincloss 的三张照片：

| | 三张的 P(attractive) | 极差 |
|---|---|---|
| 不对齐（整图拉伸到 224） | 0.679 / 0.0972 / 0.7685 | **0.671** |
| 对齐后 | 0.7458 / 0.5308 / 0.6136 | **0.215** |

极差降到原来的 1/3。剩下的差异来自那三张图本身的构图和清晰度不同
（其中一张是低分辨率方图）。

### ⚠️ 裁剪系数 2.8 是估算的，不是拟合的

原作者**没有公布训练集的裁剪方式**，所以没有任何标注可以对照。
2.8 是按人脸解剖比例推的：脸高（发际线到下巴）≈ 1.4×瞳距，脸宽（两颧）≈ 1.6×瞳距，
取 2.8 能把整个头装进去还留出四周余量。

**将来如果拿到训练集的裁剪统计，应该用那个数字替换它**，并重跑上面两张表。

### 推理封装（`src/model/classifier.ts`）

后端选择和 MediaPipe 那边同一套思路：有 `navigator.gpu` 就把 webgpu 和 wasm
各建一次、各测一次速（预热 1 次 + 计时 2 次），快的留下，另一个 release。
WebGPU 建不起来（没适配器）不算错误，只是不参与比较。

**已核实的行为（2026-09-12，本机 headless Chromium）**：

- `'gpu' in navigator` 是 **true**，但 `requestAdapter()` 返回 **null** ——
  所以「有 navigator.gpu」**不等于**「能用 WebGPU」。
  只按 `navigator.gpu` 判断会走到一个必然失败的建会话路径，
  所以代码是「尝试 → 失败就跳过」而不是「先判断再决定」；
- 回退路径实测走通：webgpu 抛 `Failed to get GPU adapter`，
  被 catch 掉、打一条 warn、继续用 wasm，**不影响出分**；
- 整条加载（下载 54MB + 建会话 + 测速）7.6 s，纯推理 **1.87 s**（单线程 WASM）；
- ⚠️ **WebGPU 这条路在本机测不了**（没有适配器），和「本机 GPU 性能数据不可信」
  是同一个限制。要在有真实 GPU 的机器上验证。

`ort.env.wasm.numThreads = 1` 是**显式**写死的，不是靠自动退化。
GitHub Pages 没有 COOP/COEP，SharedArrayBuffer 不可用，多线程会退化成单线程 ——
但退化路径和显式单线程在内存分配、构建产物上并不完全一样。
显式写死才能保证本地和线上表现一致。

### 已核实：JS 预处理与 Python 端数值一致

`src/model/classifier.ts` 的归一化（`x/127.5 - 1`）和 Python 端
`AutoImageProcessor` 必须给出同一个数，否则模型在网页上和在标定脚本里
就不是一个东西。用 37 张样本对跑（JS 走 int4 + 浏览器缩放，
Python 走 fp32 + PIL 缩放）：

| 统计量 | 值 |
|---|---|
| 样本 | 37 / 37 全部匹配 |
| \|ΔP(attractive)\| 中位 | 0.0097 |
| \|ΔP(attractive)\| 最大 | 0.0453 |

量级是对的 —— int4 量化**本身**就有 |ΔP| 中位 0.0039 / 最大 0.0314 的误差
（见上面的量化表），剩下的差额来自两边缩放实现不同
（canvas 的双线性 vs PIL 的 resample）。**不是预处理对不上**：
真对不上的话差值会是 0.1 以上量级。

## 模型资产管理

### 权重默认不入库，但有一个显式例外

`.gitignore` 排除 `*.onnx` / `*.safetensors` / `*.pth` / `*.bin`。
理由：仓库体积，以及避免把许可证有疑问的二进制提交进公开仓库。

**唯一的例外**：`public/models/attractive.int4.onnx`（54 MB，路线 B 的量化模型）。
它是**部署产物**，随站点一起发布。放行的理由和代价：

- ✅ 零第三方依赖。不依赖 HF / jsDelivr 能不能访问 —— 对国内网络环境这点很重要；
- ✅ 本地开发和线上走**完全同一条路径**，没有「本地能跑线上不行」的可能；
- ⚠️ 仓库多 54 MB，**每次 clone 都要下**（这是选这条路的主要代价）；
- ⚠️ GitHub 对单个 >50 MB 的文件会给警告（硬上限是 100 MB，54 MB 能正常推送和服务，
  但会收到一封警告邮件）。如果哪天想消掉这个警告，把文件挪到别处、
  再把 `classifier.ts` 里的 `MODEL_MIRRORS` 填上即可，前端逻辑不用动。

中间产物一律不进仓库：fp32 的 327 MB、int8 的 83.6 MB、原始 safetensors，
都只留在 `tmp/models/`。

### 加载策略：同源优先 + Cache API

`src/model/cache.ts` 负责取字节，`classifier.ts` 负责建模。

- 候选地址**按优先级**依次尝试：同源 `public/models/` → `MODEL_MIRRORS`（默认空）。
  `MODEL_MIRRORS` 留着一个空口子，是为了将来挪文件时不用改前端逻辑；
- **用 Cache API 而不是 HTTP 缓存**：HTTP 缓存什么时候过期、会不会命中，
  完全取决于服务器响应头，我们控制不了（GitHub Pages 的静态资源缓存期很短，
  只有几分钟量级）。Cache API 是我们自己命名、自己查、自己删的，命中时不发任何请求；
- 实测（本机，dev server）：首次 542 ms（走网络）→ 再次 62 ms（走缓存），**快 8.7 倍**。
  模型 54 MB，没有这层缓存等于每次刷新重下；
- Cache API 只在**安全上下文**（https / localhost）可用。局域网 IP + http 访问时
  `caches` 是 undefined，会退化成「每次重新下载」——**不报错、功能不受影响**，只是慢。
  不要把它当致命错误处理。

### ⚠️ 必须校验「拿到的是不是模型」，不能只看状态码

**这个坑已经踩过一次，且是同一类问题里的第二种。**

不少静态服务器对不存在的路径返回 **200 + index.html**，而不是 404
（Vite dev server 默认就这么干，各种 SPA 配置也会）。这种情况下：

- `res.ok === true`、`Content-Length` 是个正常数字、响应体非空 ——
  光看状态码**完全发现不了问题**；
- 结果是把一个 HTML 页面当成 54 MB 的模型缓存下来，然后 ORT 报一个
  和真实原因毫无关系的解析错误；
- 更糟的是它**进了缓存**，之后连网络都不会再走，问题被永久固化。

所以 `cache.ts` 里有两道校验，**过不了就不准进缓存，继续试下一个地址**：

| 校验 | 判据 | 挡住什么 |
|---|---|---|
| 不是文本 | 跳过开头空白后，第一个字节不是 `<` | HTML / JSON 错误页 |
| 体积下限 | ≥ 1 MB | 空响应、被截断的下载、指错文件 |

缓存**读出来的**那份也要验，验不过就删掉重下 —— 让缓存能自愈，
而不是把用户永久卡在一个坏缓存上。

⚠️ 这和本文件里记的图片那个坑（`InvalidStateError: could not be decoded`
其实来自 404 的 HTML 页）**是同一类问题**。规律是：
**看到解析/解码类报错，第一件事是查拿到的到底是什么内容，不要先怀疑格式。**

### CI 里挡一道

`deploy.yml` 有一步 `Verify model asset`：检查 `dist/models/attractive.int4.onnx`
存在，且体积在 20-90 MB 之间。

理由和 base path 那步一样 —— **漏掉模型不会有任何构建报错**，
只会在用户点开页面时静默失败。这种「构建全绿但功能废掉」的错误必须在 CI 挡住。

### MediaPipe 那边不落仓库

wasm 与 `.task` 走 jsDelivr / Google 官方 CDN，版本号写在 URL 里，
跟着 npm 包的版本一起升。

## 摄像头实时（`src/camera/` + `src/live/` + `src/ui/live.ts`）

**它是一个取景器，不是第三种测量。** 界面上、文档里都必须这么说，理由在下面每一条里。

实时循环里跑的是一份**精简版**：逐帧检测 → 质量校验 → 打分 → 平滑；跳过模糊拒判、
不跑路线 B。用户觉得取景可以了，点「冻结这一帧」，那一帧走的是**和上传照片一模一样的**
完整流程（完整质量校验含模糊、分项报告、对齐、模型卡片）。

实测同一帧两条路的综合分差 **中位 1.87、最大 3.68**（见下文交叉核对），
所以「冻结之后的分数可能和这里差几分」不是客套话，是必然的。

### 必须单建一个 VIDEO 实例，不能把单例翻成视频模式

`face/landmarker.ts` 的 IMAGE 实例是模块级单例，且 `detect()` / `detectForVideo()`
**互相有硬性模式检查，传错直接抛**。而上传 tab 的 file input 和拖放监听始终活着 ——
只要实时循环把单例翻成 VIDEO，冻结帧的 `detectFaces` 会立刻抛，之后上传任何照片也全废。

`setOptions()` 切模式又是异步且无串行化的，循环恢复和冻结并发时会互相踩；还有创建顺序
陷阱：`loadLandmarker({delegate:'auto'})` 内部要用 IMAGE 模式的 `detect()` 做后端测速，
提前翻模式会让两个后端都测速失败，报出误导性的「两个后端都初始化失败」。

**做法**：另建一个创建时即带 `runningMode: 'VIDEO'` + `numFaces: 1` 的实例，
复用已经选定的后端（`getActiveDelegate()`），不重复测速。顺序是先 `await loadLandmarker(...)`
（IMAGE，含测速），再懒创建视频实例。

`numFaces: 1` 是个必要的取舍：`checkQuality` 对「多于一张脸」是**整帧拒判**（对静态照片
是对的）。视频里墙上海报、反光、路过的人会让帧级拒判反复触发、不断清空平滑窗口，
分数永远稳不下来，而界面会暗示是用户自己的问题。视频实例只取最主要那张脸，
界面明说「只测量画面中最主要的一张脸」。

### 时间戳：只用一条时钟，跨重启不重置

MediaPipe 的 C++ graph 强制**严格递增**时间戳，违反是**硬错误**：graph 卡死，
之后每次 `detectForVideo` 都抛，直到重建实例。两个最容易踩的：混用 rVFC 回调的 `now`
（performance 时间线）与 `metadata.mediaTime`（媒体时间线，从 0 起）；以及流重启后
计数器归零，而 graph 还记着旧的水位 ——「关闭摄像头 → 再开」正好命中。

全程只用 rVFC 的 `now`，兜底 `performance.now()`，**绝不用 `mediaTime` / `currentTime`**；
`ts = Math.max(raw, lastTs + 1)` 防同毫秒碰撞（页面不是 cross-origin isolated，
`performance.now()` 被钳到 100µs，碰撞够得着）；计数器跨流重启**不重置**；
连续失败 3 次就停下并重建实例，不空转。

### 平滑：中位数 + MAD×1.4826，稳定要按**时间**判

`src/engine/smoothing.ts`，纯函数、零依赖。

- 值：滚动窗口**中位数**（抗毛刺），再过一道短 EMA（裸中位数是阶梯状输出，半窗翻转才跳
  一次，看起来像卡顿）。
- 离散度：**MAD × 1.4826**（稳健 σ），不用 p90−p10 —— n=30 时 p90 是第 3 大样本，
  被尾巴主导。稳健 σ 的关键好处是**和文档里 ±5~8 分的噪声同量纲**，所以界面可以直接写
  「±X 分」，不必另造一把尺子。
- 窗口：时间 2.5 s 与样本数 30 双封顶。
- 显示取整。CLAUDE.md 反复说不要展示个位数分差，显示「78.4」再跳到「77.1」直接违背这一点。

**稳定阈值是夹具实测定的，不是填的**（`tmp/live/`，假摄像头，各 25 s）：

| 夹具 | `±` 最小 | 中位 | 最大 | 判「稳定」的样本 |
|---|---|---|---|---|
| 静止肖像循环（11.6 Hz） | 0 | 0 | 0.1 | 30 / 40 |
| 缓慢旋转 ±12°（9.3 Hz） | 0 | 1.4 | 5.1 | 0 / 40 |

静止那一档的 0.1 分就是**检测器抖动的地板**（关键点是逐帧抖的，引擎本身确定性），
离阈值 3.0 很远，所以不会误伤端住不动的人。旋转那一档 0/40 主要不是被 σ 拦下的
（它 75% 的样本 ± ≤ 3.0），而是被姿态判据拦下的 —— 这是刻意的。

⚠️ **「稳定」的判据必须是时间跨度，不能是样本数。** 这条是实测改过来的：
原来写的是「样本数 ≥ 窗口容量的 60%」，看着等价，实际把结论绑死在帧率上 ——
静止夹具 12.0 Hz 时判稳定，掉到 **6.2 Hz** 之后同一个画面、同一组分数（±0.0，一个样本
都没变）徽章翻成「波动中」并一直卡住：2.5 s 的窗口在 6.2 Hz 下只装得下 15 帧，
够不到 18 帧的门槛。按时间判（≥ 1500 ms，即 2500 ms 的 60%）就没有这个问题。
**和下面姿态判据踩的是同一类坑：门槛卡在没有意义的维度上，于是变成死区而不是保护。**

### 姿态判据判的是**漂移**，不是绝对角度 —— 这是本模块最容易做错的一处

绝对角度阈值（比如 6°）是个**死区**：一个稳定地歪着 10° 的人，每帧都被判不合格，
窗口被反复清空，界面永远停在「正在测量…」，而用户看不出原因。

真正要防的是**缓慢转头**：中位数会被一帧帧合法的新样本带着从 80 走到 70
（文档实测 0°/10°/20°/-10° 得 80.1/81.2/76.7/70.0，**全都过 20° 那道网**），
而每帧单独看都正常、离散度也小。

所以有两道，各管一件事：

| 常量 | 值 | 作用 |
|---|---|---|
| `POSE_DRIFT_DEG` | 5 | 相对**窗口起点**的漂移，超了就**清空窗口** |
| `POSE_TRUST_DEG` | 8 | 窗口内出现过超过这个角度就不给「稳定」徽章（窗口不废） |

5° 的依据：0° 与 10° 相差 1.1 分，所以 5° 对应半分以内，在 ±5~8 分的噪声之下可以接受。
瞳距相对变化 > 5% 同样清空窗口（人往前凑了）。

实测（合成序列）：`hold10`（稳定地歪 10°）窗口能攒满 30 帧、稳；`hold4` 稳；
`sway8`（±8° 来回）窗口 size 恒为 1 —— 三者行为都符合预期。

### 模糊：抽帧测，只当提示、不拒判

模糊是本项目里唯一**不会自然暴露**的坏输入，而**稳定度指示对它结构性失明**：
一个昏暗房间里静止不动的人会得到一个又稳又错的分。所以实时模式不能把这一关砍掉。

做法：实时关卡调**现有的** `checkQuality(faces)`，**不传位图** —— 它天然跳过模糊
（`quality.ts` 里那个 `if (image)`），因此 `quality.ts` **一行都没改**。
模糊另用已导出的 `measureSharpness(bitmap, face)` 每 **1000 ms** 抽一帧测（按时间节流，
不按帧数：检测速率跨设备能差 3 倍，按帧数算会变成慢设备 0.3 Hz、快设备 2 Hz），
结果**只喂给提示**，带 0.5 的死区和 2 次采样的停留（约 2 s），否则阈值附近会以 1 Hz 闪提示。

实测证据（`blur.y4m`）：实时读数 **74、±0.0、徽章「稳定」**，同时提示
「画面略微发虚，读数会跳得比清晰时大」（清晰度 4.1）；冻结同一帧得 78 并带
「照片略微发虚」的警告。**这就是「稳定但错误」真实存在的证据** ——
将来有人问「为什么实时模式在糊画面上还给数字」，拿这一条回答。

### 实测数据（本机，假摄像头，SwiftShader）

| 夹具 | 实时 fps | 清晰度 | 实时读数 | 冻结果 |
|---|---|---|---|---|
| `static.y4m` 静止肖像 1280×720 | 11.6 Hz | 9.5 正常 | 69，±0.0，稳定（30/40） | 综合分 71 + 「人脸偏小（瞳距约 60px）」 |
| `rotate.y4m` ±12°，8 s 周期 | 9.3 Hz | 6.7 正常 | 70–75 游走，**稳定 0/40** | 77 + 人脸偏小的警告 |
| `blur.y4m`（CSS `blur(2px)`） | 6.9 Hz | 4.1 略软 | 74，±0.0，**稳定** + 发虚提示 | 78 + 发虚警告 |
| `small.y4m` 640×480 | 11.3 Hz | 4.5 略软 | **拒判**：提示「请离镜头近一点」 | **拒判**：「瞳距约 40 像素，需要至少 50」 |

首次出数要 8 秒上下（MediaPipe 的 wasm 下载 + 双后端测速占绝大部分），之后才进入稳态。

**冻结帧的清晰度没有出现系统性拒判**（这一条是专门查过的，因为 4.0 是在 37 张
**专业肖像**上标定的、而摄像头帧整体更软）：四个夹具的最低值是 640×480 那档的 **4.5**，
仍在阈值 4.0 之上，只有「略软」的警告；静止的 1280×720 是 9.5。也就是说
**不要**因为「摄像头比专业照片软」就去调 4.0 —— 现有数据不支持这个改动。

### ⚠️ 能力边界：`minIpd = 50` 会咬 640×480 的笔记本摄像头

`small.y4m` 是 640×480，瞳距只剩 **41 px**，实时和冻结**都拒判**。这不是 bug，
是 `quality.ts` 的绝对值阈值（`minIpd: 50` / `warnIpd: 100`）在低分辨率下的必然结果：
它量的是「脸有多少像素」，而脸的像素数正是关键点精度的决定因素，所以**保持绝对值**
（不改成帧宽比例）是刻意的。代价要写清楚：**用 640×480 摄像头、又坐得稍远的用户
会长期落在警告区甚至拿不到分**，界面必须告诉他靠近，而不是给一个不可信的数。

### 同一帧走两条路的交叉核对：差得不是零，而且**不该**追求是零

同一帧分别走实时（`detectForVideo`）和冻结（`detect`），37 组比对：

| 量 | 值 |
|---|---|
| 综合分 \|Δ\| 中位 / 最大 | **1.87 / 3.68** |
| IMAGE 侧重复运行 | 逐位相同（71.26） |
| VIDEO 侧 | 逐帧变化（时序跟踪的 ROI） |

分项里差得多的是 `jawOverFaceWidth`（Δ 分最多 30）、`faceWidthOverHeight`（约 19）、
`interCanthalOverIPD`（约 -23）、`noseOverInterCanthal`（约 -14）；
`upperOverMiddle`、`lowerOverMiddle`、`mouthOverNose` 挨得很近。

原因不是 bug：**VIDEO 模式跑在检测器给的时间域跟踪 ROI 上，和单帧 IMAGE 推理本来
就不是同一次测量**。所以「实时」和「冻结」的分差要如实写在界面上，以冻结为准。

### 冻结 + 路线 B 的状态机：只有 classify 那一段锁界面

`cl.classify()` 在 `numThreads = 1` 下是**主线程同步**的，本机实测 **2.4~2.5 s**
（PerformanceObserver 长任务条目），期间整个页面都不响应。所以：

- 这一段必须把实时面板锁住、并明说「学习模型正在推理，实时测量已暂停 —— 大约两秒」；
  DOM 改完之后要**让出一次任务**（`setTimeout(0)`）再进 classify，否则这次改动
  根本没机会上屏，用户看到的是界面直接卡住而不是「正在推理」。已验证：外部探针
  能在这段窗口里读到 disabled 的按钮和那句状态文字。
- **下载 54 MB 那一段不能锁**。主线程是空闲的，慢网络下要几分钟，锁掉等于把用户
  钉死在冻结态里。实测踩过这个坑：CDN 上的 `ort-wasm-*.wasm` 有一次拿到 200 之后
  响应体再也没下完，界面就那样挂着，除了刷新页面没有别的出路。
- 那段时间「返回实时」是**开着**的，而且点了真的取消得掉：`backToLive` → `reset()` →
  `replaceAligned` → 令牌自增，`runModel` 在 classify 之前的那次 `alive()` 检查就过不去。
  实测：点一下之后全程长任务 **0 个**、evaluate 往返最大 **94 ms**；不取消的那次则是
  一个 2497 ms 的长任务。这是同一段代码的 A/B 对照。
- ⚠️ 诚实的一条：**收完 54 MB 的那一刻**主线程会停约 **1.6 s**，取消也避不开。
  原因在 `cache.ts`：`readWithProgress` 要把上百个分片拼成一个连续 ArrayBuffer，
  `writeCache` 又 `slice(0)` 了一份，两次 54 MB 的同步拷贝。它和 classify 是两回事，
  记录在此，没有动它（改动要重新验证 54 MB 那条缓存链路）。

另外 `#result` 是单个顶层区块，冻结结果和实时读数同屏就是**两个分数并排**，
正是首页刻意避免的。所以冻结时实时读数隐藏，用「返回实时」清掉，结构上只有一个分数。

### 不镜像，理由是「代价最小」，不要编造别的

**先前设想的理由（「canthal tilt 这类有方向的指标会翻转」）是错的**：`engine/metrics.ts`
里 7 个指标全是 `dist`（`Math.hypot`，无符号）和沿轴投影的比值，**没有任何一个是符号敏感的**，
引擎里也没有 canthal tilt 这个指标。而且只镜像**显示**根本不改变检测器的输入。

真实理由：**叠加层坐标与上传路径 1:1 对应，不需要任何翻转映射**。就写这个。

### 预览按检测速率刷新，不做「原生视频 + 透明叠加」

`drawOverlay` 会先 `clearRect` 再整帧 `drawImage`，画布是**不透明**的，
「原生 `<video>` 平滑播放 + canvas 只叠关键点」这个设想不成立（canvas 会把 video 全盖住）。
而真正的透明叠加层有另一个问题：关键点是 32–42 ms 之前那一帧算出来的，
**会明显滞后于正在播放的画面**，用户会拿关键点去对脸上的特征。

做法是**一个画布，每帧整帧绘制，预览就是检测速率**：你看到的那一帧就是被测量的那一帧。
代价是预览不如原生视频顺滑 —— 刻意选的取舍。

叠加层用的 478 个点做了 EMA（α=0.4，约 2 帧滞后）：每帧画原始点会明显抖动，
而用户对画面抖动的怀疑远大于对数字的怀疑。⚠️ **平滑只作用于画出来的点**，
分数用的始终是这一帧的原始关键点。

### 无障碍：读数区绝不能是 live region

读数 10+ Hz 更新一次，放进 `aria-live` 会把读屏软件刷爆。所以读数/说明/调试行都是
`aria-live="off"`，**只有取景提示那一行**走 `polite`（它带 400 ms 停留，本来就低频）。

### 实测方法：假摄像头夹具（`tmp/live/`，不入库）

本机没有真摄像头，但可以喂 Playwright 的 Chromium 一个真人的 y4m：

```bash
chromium --use-fake-ui-for-media-stream --use-fake-device-for-media-stream \
         --use-file-for-fake-video-capture=tmp/live/static.y4m
```

夹具由 `tmp/faces/` 的公有领域肖像 + ffmpeg 的 `loop`/`rotate`/`scale`/`yuva420p` 生成
（`tmp/live/make_fixtures.sh`），探针是 `tmp/live/*.mjs`：`e2e_probe.mjs`（走一遍真实界面）、
`cross_probe.mjs`（同一帧两条路）、`longtask_probe.mjs`（主线程长任务）、
`cancel_probe.mjs`（下载途中取消）、`pose_probe.mjs` / `semantics_probe.mjs`（姿态、关键点语义）。

⚠️ **量「主线程有没有被独占」要用 PerformanceObserver 的 `longtask` 条目**，
不能只看 `evaluate` 往返延迟 —— 后者还会被 GC、CDP 排队影响，而前者不会。
两个信号一起看才分得清「真堵住」和「只是慢了一下」。
反过来，`longtask` 也会漏（收完 54 MB 那次 1.6 s 的停顿它就没报），
所以量「渲染主线程被占住多久」还得多一条**页面内 `setInterval(10ms)` 的最大间隔**。

⚠️ 夹具探针跑起来慢（每次都要重下 MediaPipe 的 wasm、重测后端），
一次要 30–50 s 才出数，采样循环的等待上限别写 30 s 就以为够。

## 目录结构（约定）

```
good-looking/
├── index.html
├── src/
│   ├── main.ts              # UI 入口
│   ├── app.ts               # 装配层：把界面事件接到 face/engine 上
│   ├── face/                # MediaPipe 封装：检测、关键点、姿态、质量、对齐
│   ├── engine/              # 纯 TS 几何打分引擎，零依赖、无 DOM，必须可单测
│   ├── camera/              # getUserMedia 生命周期（权限、断流、错误映射）
│   ├── live/                # 实时：帧循环（loop.ts）+ 逐帧判定（session.ts）
│   ├── model/               # onnxruntime-web 推理封装（路线 B）
│   └── ui/                  # 界面层：叠加层、报告、模型卡、实时面板
├── scripts/
│   ├── calibration/         # 几何指标 + 清晰度阈值的校准流程（Python + 浏览器)
│   └── model/               # 路线 B：导出 → 量化 → 校准校验（Python + 浏览器）
├── public/                  # 静态资源；models/ 不入库
├── tmp/                     # 临时产物，不入库（含 venv-model、样本、ONNX 中间产物、live/ 夹具）
└── .github/workflows/       # GitHub Actions 构建 + 部署 Pages
```

### 路线 B 的流水线顺序

```
export_onnx.py        →  tmp/models/attractive.onnx           （导出 + 数值一致性验证）
quantize_onnx.py      →  tmp/models/attractive.int4.onnx      （量化 + 排序一致性验证）
score_corpus.py       →  模型输出分布的基线（回答「能不能当分数用」）
align_probe.js        →  对齐效果的验证（回答「对齐有没有用」）
classifier_probe.js   →  网页端封装的输出
  └ verify_js_vs_py.py → 与 Python 端比对（回答「预处理有没有写错」）
```

每个脚本跑完都会打印自检结果，**看到 FAIL 或者 ρ 掉下来就先别往下走**。

## 工程约定

- **`src/engine/` 必须保持零依赖、无 DOM**：纯函数，输入关键点、输出分数。
  这样它才可单测、可复用，也是路线 A 的核心资产。
- **不做简化版**：不要为了跑通而写 demo 版 / stub 版实现，残缺实现不如不写。
- **不建测试文件目录**，除非明确要求。
- **临时文件一律放 `./tmp/`**，不要写到系统 `/tmp`。
- 修改功能时不要顺手新建无关文件。

## 常用命令

```bash
pnpm dev            # 本地开发（Vite dev server，端口 5273）
pnpm build          # typecheck + 构建到 dist/
pnpm preview        # 预览构建产物（验证 base 路径是否正确）
pnpm typecheck      # 只跑类型检查
```

部署到 GitHub Pages 时，Vite 的 `base` 必须是仓库名（见 `vite.config.ts` 的 `REPO_NAME`）。
改了仓库名要同步改那里，CI 里有一道 base 路径校验会挡住不一致的情况
（这个错误在本地 dev 下看不出来，只有部署后才暴露）。

CI 用的 Action 版本（2026-09-12 核实）：`actions/checkout@v7`、`actions/setup-node@v7`、
`pnpm/action-setup@v6`、`actions/configure-pages@v6`、`actions/upload-pages-artifact@v5`、
`actions/deploy-pages@v5`。

## 验证清单

改完推理链路后，至少确认：

1. `pnpm build && pnpm preview` 能跑通，控制台无 404；
2. 上传一张正脸照能拿到分数，且**同一张图重复上传分数一致**（几何路线必须满足）；
3. 侧脸 / 模糊 / 无脸 的输入要有明确提示，不能静默给出错误分数。

改到实时那几条链路时，另加：

4. **478 个点齐全、索引语义没串**（本文件的硬要求）：用 `tmp/live/semantics_probe.mjs`
   在真人帧上把点画出来**肉眼核对**，别只看数字。视频路径的 ROI 反归一化和 z 缩放
   与 IMAGE 路径是两套代码。
5. **VIDEO 与 IMAGE 的姿态读数一致**（`tmp/live/pose_probe.mjs`）：同一张正脸走两条路，
   `|yaw|,|pitch|,|roll|` 相差应在 2° 以内。不一致就说明 20° 那道网在实时模式下含义变了，
   建在它上面的一切稳定性结论都不成立。失败特征是 `no-pose-data` 在 100% 的帧上触发。
6. 切模式 / 隐藏页面 / 关页面之后 `track.readyState === 'ended'`、`srcObject` 已清空
   （`e2e_probe.mjs` 会打出来）。
7. 冻结帧走完整流程：实时读数隐藏、`#result` 只有一份分数、模型卡按状态正确切换
   （`longtask_probe.mjs` / `cancel_probe.mjs`）。
